name: snakemake
channels:
  - conda-forge
  - bioconda     # Recommended for bioinformatics workflows
  - nodefaults   # Explicitly prevents searching the 'defaults' channel
dependencies:
  - python=3.11
  - snakemake=8.* # Changed from -minimal (see note below)
  - mamba